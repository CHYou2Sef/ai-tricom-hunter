# ╔══════════════════════════════════════════════════════════════════╗
# ║  requirements-prod.txt                                           ║
# ║  Streamlined dependencies for production (Docker) environments   ║
# ║  Excludes local dev tools, benchmark tools, and optional/bloated ║
# ║  engines (like Camoufox or raw Selenium) to minimize footprint   ║
# ╚══════════════════════════════════════════════════════════════════╝

# Core data processing
pandas>=2.2.0

# The Primary Browsers (Patchright, Nodriver, Crawl4AI)
patchright>=1.58.2
nodriver>=0.48.1
Crawl4AI>=0.8.6
httpx[http2]>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# LLM Fallback (Minimal client)
google-generativeai>=0.4.0

# Utility & Parsing
python-dotenv>=1.0.0
colorama>=0.4.6
openpyxl>=3.1.2
