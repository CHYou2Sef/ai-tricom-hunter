# ╔══════════════════════════════════════════════════════════════════╗
# ║  requirements.txt  —  All Python dependencies                    ║
# ║                                                                  ║
# ║  Install everything with one command:                            ║
# ║      pip install -r requirements.txt                             ║
# ║                                                                  ║
# ║  After installing, also run:                                     ║
# ║      patchright install chromium   (Tier 1 — Chrome patché)     ║
# ║      python -m camoufox fetch      (Tier 4 — Firefox custom)    ║
# ╚══════════════════════════════════════════════════════════════════╝

# ── Browser Automation (The Hybrid Engines) ───────────────────────────
# Tier 0: Selenium + Undetected-ChromeDriver (Benchmark Arena Engine)
selenium>=4.18.0
webdriver-manager>=4.0.1
undetected-chromedriver>=3.5.5
# Tier 1: Patchright — Drop-in replacement for Playwright, patched for stealth.
#   Passes: Cloudflare, Kasada, Akamai, Datadome, Fingerprint.com, Sannysoft.
#   API: 100% identical to Playwright — only the import line changed.
patchright

# Tier 2: Nodriver (Stealth CDP-only agent, zero WebDriver fingerprints)
nodriver>=0.34
websockets>=13.0

# Tier 3: Crawl4AI (Managed scraper, bypasses WAF, markdown support)
crawl4ai>=0.4.0

# Tier 4: Camoufox — Patched Firefox anti-detect browser (last resort, Firefox engine)
#   After install: python -m camoufox fetch  (downloads the custom Firefox binary ~200MB)
# camoufox (Removed for light version)


# ── Data Extraction & Parsing ──────────────────────────────────────────
# Fast HTML parsing
selectolax>=0.3.21
beautifulsoup4>=4.12.3
lxml>=5.1.0

# Excel File Handling
openpyxl>=3.1.2
pandas>=2.2.0


# ── System & Utilities ────────────────────────────────────────────────
# 24/7 Filesystem Monitoring
watchdog>=4.0.0

# Async HTTP/Network
httpx>=0.27.0
aiofiles>=23.2.1

# Environment and Config
python-dotenv>=1.0.0


# ── Quality Assurance ─────────────────────────────────────────────────
# Testing Framework
pytest>=8.0.0
pytest-asyncio>=0.24.0


# ── Quick Setup Commands ──────────────────────────────────────────────
# Step 1: pip install -r requirements.txt
# Step 2: patchright install chromium       ← Tier 1 (Chrome stealth)
# Step 3: python -m camoufox fetch          ← Tier 4 (Firefox anti-detect)
# ──────────────────────────────────────────────────────────────────────

# ollama (Removed for light version)
# code-review-graph (Removed for light version)
httpx[http2]
fastapi
uvicorn
