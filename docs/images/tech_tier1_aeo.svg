<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 500" width="800" height="500">
    <rect width="100%" height="100%" fill="#0B1F3A" rx="10" ry="10"/>
    <defs>
        <marker id="arrow" viewBox="0 0 10 10" refX="10" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
            <path d="M 0 0 L 10 5 L 0 10 z" fill="#00D1FF" />
        </marker>
        <style>
            .box { fill: rgba(0, 209, 255, 0.05); stroke: #00D1FF; stroke-width: 2px; rx: 8; ry: 8; }
            .title { fill: #00D1FF; font-family: 'Montserrat', sans-serif; font-size: 20px; font-weight: bold; text-anchor: middle; }
            .step-title { fill: #FFF; font-family: 'Inter', sans-serif; font-size: 16px; font-weight: bold; }
            .step-desc { fill: #A0C0E0; font-family: 'Inter', sans-serif; font-size: 13px; }
            .line { stroke: #00D1FF; stroke-width: 2px; fill: none; marker-end: url(#arrow); }
            .icon-box { fill: rgba(0, 209, 255, 0.15); stroke: #00D1FF; stroke-width: 1px; rx: 50%; ry: 50%; }
        </style>
    </defs>

    <text class="title" x="400" y="40">⚖️ Tier 1: AEO (Answer Engine Optimization)</text>
    <text x="400" y="65" fill="#A0C0E0" font-family="Inter" font-size="14" text-anchor="middle">Zero-Click Metadata Sniffing</text>

    <!-- Step 1: Input -->
    <rect class="box" x="50" y="100" width="700" height="80"/>
    <text class="step-title" x="70" y="135">1. Target Entity Probe</text>
    <text class="step-desc" x="70" y="160">If SGE fails, agent performs a high-precision Knowledge Panel search.</text>

    <path class="line" d="M 400 180 L 400 220"/>

    <!-- Step 2: Generation -->
    <rect class="box" x="50" y="220" width="700" height="80"/>
    <text class="step-title" x="70" y="255">2. DOM Sniffing (Schema.org)</text>
    <text class="step-desc" x="70" y="280">Worker sniffs the hidden DOM elements specifically for JSON-LD scripts.</text>

    <path class="line" d="M 400 300 L 400 340"/>

    <!-- Step 3: Parsing -->
    <rect class="box" x="50" y="340" width="700" height="100"/>
    <text class="step-title" x="70" y="375">3. Knowledge Panel Extraction</text>
    <text class="step-desc" x="70" y="400">Worker extracts the phone vector directly from the Knowledge Panel DOM Node.</text>
    <text class="step-desc" x="70" y="420">Authority: High (Google-vetted business profiles).</text>

    <!-- Artistic touch: Search icon -->
    <circle class="icon-box" cx="720" cy="40" r="25"/>
    <text x="720" y="45" font-size="20" text-anchor="middle">🔍</text>
</svg>
